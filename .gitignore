# Personal, machine-local Claude Code settings — not shared
.claude/settings.local.json

# Generated SQLite database — rebuild with `python3 load_csvs.py`
datadesk.db

# Python virtual environment — recreate with `python3 -m venv .venv && .venv/bin/pip install -r requirements.txt`
.venv/
